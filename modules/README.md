# Game Modules

This directory contains YAML game-module definitions. Each file defines a game
that the ledger can track, conforming to the module contract in `docs/module-contract.md`.

Game modules are loaded and validated against the module JSON Schema at startup.

## Structure

Each module file: `modules/<game-id>.yaml`

The Skyjo module and others will be added in later prompts.
